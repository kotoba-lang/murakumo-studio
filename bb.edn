;; babashka タスク。CSS 生成とテストを bb から回す（manimani ADR-0033 と同型）。
{:paths ["scripts"
         ;; kotoba-ui CSS 生成(bb ui-css)が読む design system 群(全て純 cljc・babashka-safe)
         "../shitsuke/src"
         "../liquid-glass-ui/src"
         "../css/src"]
 :tasks
 {ui-css
  {:doc "kotoba-ui(liquid-glass) の静的 CSS を tauri/dist/vendor/ へ生成"
   :task (load-file "scripts/gen_kotoba_ui_css.clj")}

  test
  {:doc "共有コア(models/fleet)のテスト"
   :task (clojure "-M:test")}}}
